name: aluminion_reads
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - nanoplot
  - chopper
  - pillow       # DPI metadata in NanoPlot PNG output
  - pip
  - pip:
    - kaleido    # Plotly static image export (avoids headless-browser dependency)
