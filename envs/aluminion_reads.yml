name: aluminion_reads
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - nanoplot
  - chopper
  - pillow       # DPI metadata in NanoPlot PNG output
  - pip
  - pip:
    - kaleido==0.2.1  # Plotly static image export — 1.x has incompatible API with NanoPlot's plotly
